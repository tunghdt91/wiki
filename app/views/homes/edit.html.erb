<%= form_for(@home) do |f| %>
  <% if @home.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@home.errors.count, "error") %> prohibited this category from being saved:</h2>

      <ul>
      <% @home.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <b>Title  </b>(<span style=" color: red;">*</span>):
    <%= f.text_field :title %>
  </div>
  <div class="field">
      <b>Content</b>(<span style=" color: red;">*</span>):
      <%= f.text_area :content%>
    </div>
  <div class="actions">
    <%= f.submit "Save",class: "medium warning btn pretty"%>
  </div>
<% end %>
